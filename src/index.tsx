import * as React from "react";
import * as ReactDOM from "react-dom";
import "./style.css";

import {StartComponent} from "./components/Start/StartComponent";

ReactDOM.render(
  <StartComponent />,
  document.getElementById("path-app")
);